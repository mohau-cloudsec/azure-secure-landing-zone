# Terraform files — never push these to GitHub
.terraform/
terraform.tfstate
terraform.tfstate.backup
.terraform.lock.hcl

# Variable files that may contain secrets
*.tfvars
*.tfvars.json

# OS files
.DS_Store
Thumbs.db